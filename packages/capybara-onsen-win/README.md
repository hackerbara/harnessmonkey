# Capybara Onsen (Windows)

Auto-translated from `packages/capybara-onsen` for the win32-x64 Claude Code 2.1.201 bundle by `examples/capybara-onsen-generator/translate_to_windows.py`. The Windows build minifies the shared source with different identifier names, so every anchor and every host-identifier reference was realigned via token-position alignment of the two bundles.

This package **builds through the real PE pipeline** against the pinned Windows binary (fail-closed pins satisfied, postconditions pass). **Visual rendering is unverified** — it must be confirmed by launching the patched `claude.exe` in Windows Terminal. See `WINDOWS.md`.
